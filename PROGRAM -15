#include <stdio.h>
#define SIZE 5
int hashTable[SIZE];

void insert(int key){
    int i, index=key%SIZE;
    for(i=0;i<SIZE;i++){
        int try=(index+i)%SIZE;
        if(hashTable[try]==0){ hashTable[try]=key; return; }
    }
    printf("Table Full\n");
}
void display(){
    for(int i=0;i<SIZE;i++) printf("%d ",hashTable[i]);
}

int main(){
    insert(10); insert(20); insert(30); insert(25);
    display();
    return 0;
}
