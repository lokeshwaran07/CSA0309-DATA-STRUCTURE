#include <stdio.h>
#define SIZE 5
int stack[SIZE], top=-1;

void push(int x){ if(top<SIZE-1) stack[++top]=x; }
void pop(){ if(top>=0) top--; }
void peek(){ if(top>=0) printf("Top=%d\n",stack[top]); }

int main(){
    push(10); push(20); peek();
    pop(); peek();
    return 0;
}
