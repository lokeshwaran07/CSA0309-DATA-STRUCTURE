#include <stdio.h>
#define MAX 5
int s1[MAX],s2[MAX],t1=-1,t2=-1;
void enqueue(int x){ s1[++t1]=x; }
int dequeue(){
    while(t1!=-1) s2[++t2]=s1[t1--];
    int val=s2[t2--];
    while(t2!=-1) s1[++t1]=s2[t2--];
    return val;
}
int main(){
    enqueue(1);enqueue(2);enqueue(3);
    printf("Deq=%d",dequeue());
    return 0;
}
