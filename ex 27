#include <stdio.h>
#include <string.h>
int main(){
    char s[20]="dcba",t; int i,j;
    for(i=0;i<strlen(s);i++)
        for(j=i+1;j<strlen(s);j++)
            if(s[i]>s[j]){ t=s[i]; s[i]=s[j]; s[j]=t; }
    printf("Sorted String: %s",s);
    return 0;
}
