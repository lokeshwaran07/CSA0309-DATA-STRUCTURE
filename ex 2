#include <stdio.h>
#include <ctype.h>
int main() {
    char s[50]; int i, valid = 1;
    printf("Enter string: ");
    scanf("%s", s);
    for(i=0;s[i]!='\0';i++)
        if(!isalpha(s[i])) valid = 0;
    if(valid) printf("Valid String");
    else printf("Invalid String");
    return 0;
}
