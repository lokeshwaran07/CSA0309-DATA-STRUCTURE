#include <stdio.h>
int main() {
    int a[5] = {10, 20, 30, 40, 50}, n = 30, i, found = 0;
    for(i=0; i<5; i++) {
        if(a[i] == n) { found=1; break; }
    }
    if(found) printf("Found at index %d", i);
    else printf("Not Found");
    return 0;
}
